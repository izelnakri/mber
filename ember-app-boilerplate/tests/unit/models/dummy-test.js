import { module, test } from 'qunit';
import { setupTest } from '../../helpers';

module('Unit | Model | Dummy', function(hooks) {
  setupTest(hooks);

  test('it exists', function(assert) {
    assert.ok(!!true);
  });
});
