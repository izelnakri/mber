import test from 'ava';

test('lookup(folderName, extension) works for .js and .hbs', (t) => {
  t.true(true); // TODO: do this
});
test.todo('lookup(folderName, extension) works for .hbs when there are no files');
test.todo('lookup(folderName, extension) works for .js when there are no files');
test.todo('lookup() works for .js by default');
test.todo('lookup() works for .js when there are no files');
