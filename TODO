// TODO: dependencies caching for all imports based on package.json version if dependency is provided from node_modules
// move ember-cli-fastboot, ember-data and ember-devtools to devDependencies
// check historical 'ajax-service' initializer of memserver for fastboot(its now removed)

make make all internal modules under /vendor (cost vs flexibility) too much lockdown for speed
replace rimraf with fs-extra
- add linter (js + hbs)
- rootURL in the test

All I need:
- investigate how people use engines

addons to investigate:
- ember-cli-sri, https://github.com/jonathanKingston/broccoli-sri-hash/blob/master/index.js adds integrity
- ember-cli-csp
// add generators
CDN
- allow overriding sandbox

add eslint-plugin-ember + eslint

test:
"_APPLICATION_TEMPLATE_WRAPPER": false,
"_TEMPLATE_ONLY_GLIMMER_COMPONENTS": true

- write appTransform tests
