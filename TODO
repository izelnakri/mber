// ml-button/component  + ml/button/template

- maybe I need proper mber version use check depending on the path

ember-resolver, ember-load-initializer(1 module), ember-inflector, ember-data

simplistic terms.

// PARCEL TRIES TO LOOKUP ember-data
// frontend files + templates + fastboot + testing + prepend/append + addon system

All I need:
- use amd-name-resolver for frontend code?
- How to do hbs compiling
- Ignore ember-cli-build but yet hack all the needed things (vendor + application.js separation):
 - add ember-cli-*plugins interop
- Ember resolver logic(app.js)
- Working with existing plugin system
- Testing commands
- ember build command with environments
- figure out a way to run fastboot
















check ora cool library for terminal spinner
